on: push
name: Main
jobs:
  prepare:
    name: prepare
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - run: ls -la ./scripts/changed-files.js
    - name: Cache node modules
      uses: actions/cache@v1
      with:
        path: node_modules
        key: ${{ runner.OS }}-build-${{ hashFiles('**/yarn.lock') }}
    - name: Install
      uses: CultureHQ/actions-yarn@master
      with:
        args: install
  lint:
    name: lint1
    needs: prepare
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Cache node modules
      uses: actions/cache@v1
      with:
        path: node_modules
        key: ${{ runner.OS }}-build-${{ hashFiles('**/yarn.lock') }}
    - name: Lint
      uses: CultureHQ/actions-yarn@master
      with:
        args: test:u
  lint2:
    name: changed-files
    needs: prepare
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Cache node modules
      uses: actions/cache@v1
      with:
        path: node_modules
        key: ${{ runner.OS }}-build-${{ hashFiles('**/yarn.lock') }}
    - name: testrun
      run: git diff --name-only origin/master
    - run: npm run test:file
